1.redis可以存储常见的数据结构有哪些？
    String、hash\list\set\zset
2.redis中list、set有什么区别？
    list 有序、元素可重复是一个双向链表结构可以从头部尾部添加删除元素，可以作为栈也可以作为队列队列
    Set 无序、元素不可重复 是通过hash table实现添加、删除、查询时间复杂度都是O(1)

3.缓存穿透、缓存击穿、缓存雪崩
    缓存穿透：缓存中不存在、数据库中也不存在导致频繁查询数据库
            解决方案：1）不存在也放缓存；2）使用布隆过滤器；

    缓存雪崩：大量key同一时间失效，导致请求都打到DB上，造成DB不可用
            解决方案：1）热点数据不过期；2）缓存失效时间尽量错开；3）采用多级缓存机制如：redis->memcache->DB

5.布隆过滤器原理

6.redis是如何保证一致性

7.redis持久化原理
    aof机制：append only file增量备份可以手动、也可以配置
    RDB机制：全量备份，数据经过压缩存储比较紧凑，可以手动也可自动
8.redis内存淘汰策略
    淘汰策略一共有六种：
    设置有效期：1.设置有效时间的最近最少使用的淘汰；
                2.设置有效期的将要过期的淘汰；
                3.随机淘汰
    所有的keys: 4.最近最少使用的淘汰；
                5.随机淘汰
                6.不淘汰
